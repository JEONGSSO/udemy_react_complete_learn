import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';

import { Shop } from '../index';

/**
 * @jest-environment jsdom
 */

describe.skip('전자상거래 앱 테스트', () => {
  // beforeEach(() => {
  //   render(<Shop />);
  // });
  it('test', () => {
    expect(true).toBe(true);
  });
});
